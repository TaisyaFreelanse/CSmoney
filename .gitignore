.DS_Store
Thumbs.db

# локальные env
web/.env
web/.env.local
web/.env.*.local

# SSH keys in repo root (never commit private keys)
/id_ed25519
/id_rsa
*.pem

# one-off Steam profile export (do not commit)
/acc1-for-render.tar.gz
